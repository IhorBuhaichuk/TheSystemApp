# P01 Evidence: Safe History Edits

- Date: 2026-09-19
- Branch/HEAD: `codex/overnight-premium-hud-polish` / `184c0044718e22b97d01cee20f0da707a355c8bc`
- Dirty scope: P01 code/tests/docs plus pre-existing uncommitted audit/prompt documentation.
- Risk: AUD-01, destructive exercise edit inside a multi-exercise workout session.

## Result

- Manual log creation now passes `sessionId = null` and always creates a new session; it no longer guesses the first session for a date.
- Existing-session edits require explicit `sessionId + exerciseId` in both write paths.
- New rows in the two manual logging dialogs are explicitly completed; persisted incomplete historical sets remain incomplete and cannot advance weight/rank.
- `WorkoutAnalyticsDao.replaceExerciseSets` runs in a Room transaction, replaces only target exercise sets, recalculates completed weighted tonnage across the whole session, and updates the parent via `@Update`.
- Session ID, quest ID, timestamp, cycle day, duration, sibling exercises, bodyweight/time sentinel sets, and incomplete sets are preserved as applicable.
- Room schema remains version 51; no migration or schema JSON change was needed.

## Verification

- Targeted JVM: three P01 test classes (`LogWorkoutSets`, `SaveExerciseSets`, repository) -> 6/6 PASS.
- Compile gate: `scripts/check-quick.cmd` -> PASS.
- Android-test compile: `:app:compileDebugAndroidTestKotlin` -> PASS.
- Full host suite: `scripts/check-tests.cmd` -> 303 tests, 0 failures, 1 skipped.
- Room guards: `scripts/check-room.cmd` -> PASS.
- Device Room test: `:app:connectedDebugAndroidTest -P...WorkoutAnalyticsEditIntegrationTest` -> 1/1 PASS.
- Device: Realme C55 `RMX3710`, serial `HQVCFY6PVOHUFI6T`, Android API 35; in-memory database, no app-data clear/import/uninstall.

## Acceptance

- A/B/C edit isolation, two same-day sessions, repeated edit, incomplete-set tonnage, parent metadata, and forced rollback: covered and passing.
- No blockers remain for P01. Next dependency-safe task: P02.
